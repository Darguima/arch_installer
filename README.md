# Arch Installer

My Arch `json` profiles for `archinstall` alongside the Python server that I use to expose them online, at my website [not available yet :) ].
This server is used to easily access and merge configurations - defaults, laptop and desktop configurations.
